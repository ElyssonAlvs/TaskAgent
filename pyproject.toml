[project]
name = "taskagent"
version = "0.1.0"
description = "AI-powered task manager using natural language"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=2.33.0",
    "requests>=2.31.0",
]

[project.scripts]
taskagent = "src.main:main"

[tool.uv]
dev-dependencies = []
